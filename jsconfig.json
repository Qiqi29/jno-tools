{
    "compilerOptions": {
        // 指定源代码根目录的基路径，用于解析非相对模块标识符
        "baseUrl": "./",
        // 路径映射，使用 @ 符号表示根目录
        "paths": {
            "@/*": [
                "src/*"
            ]
        },
    }
}